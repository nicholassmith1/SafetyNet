
import numpy as np
def find_pedestrians(frame_cur):
    """
    @returns    a Nx4 numpy array, in which each row represents a pedestrian
                discovered in frame_cur, with the columns representing the
                (x, y, width, height) of the bounding box of the pedestrian.
    """
    # TODO - replace stub
    return np.empty((0, 4))